NODE_ENV=development
PORT=5000

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tapme_db
DB_USER=tazio
DB_PASSWORD=tazio4321

# File uploads
UPLOAD_DIR=uploads

# CORS — comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# Frontend public URL — used in emails to reference static assets (logo, images)
FRONTEND_URL=http://localhost:3000

# SMTP — Gmail / Google Workspace
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=info@tazio.in
SMTP_PASS=sngvuruutkehryww
EMAIL_FROM="TapMe Labs <info@tazio.in>"

# Razorpay
RAZORPAY_KEY_ID=rzp_test_SqNtgNQO00HwIS
RAZORPAY_KEY_SECRET=kxo1qmEtOShJ7X2ep0vNIYna
